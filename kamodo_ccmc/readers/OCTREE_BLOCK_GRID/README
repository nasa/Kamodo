Implementation of octree block interpolation and field line tracing based on CCMC library of C functions that have been used in online visualization since 2000.

The shared library is compiled automatically during ``pip install`` (see setup.py).
It builds on Linux, macOS, and Windows for x86-64 and ARM architectures.
